{
  "scripts": {
    "format": "prettier --write spec && editorconfig-cli -x '/gradle.*' && markdownlint README.md"
  },
  "editorconfig-cli": [
    "*",
    "backend/**/*",
    "frontend/**/*",
    "spec/*"
  ],
  "devDependencies": {
    "@htmlacademy/editorconfig-cli": "^2.0.4",
    "markdownlint-cli": "^0.35.0",
    "prettier": "^2.8.8"
  }
}
